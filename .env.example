# Copy this to .env and fill in your values before running docker compose up.

# Public hostname or IP of the server.
# Local dev:  API_HOST=localhost
# Digital Ocean:  API_HOST=<your-droplet-ip>
API_HOST=localhost

# Database credentials (defaults work with docker-compose as-is)
DB_USER=sentinel
DB_PASSWORD=sentinel
DB_NAME=sentinel
